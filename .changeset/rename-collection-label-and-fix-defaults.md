---
"emdash-notion": patch
---

管理画面の「EmDash Collection」「Notion Database」ラベルを分かりやすくし、Notion データベースのセレクトを「DB名 (id)」形式に変更。また、本文/著者/slug フィールドの既定値を実際の emdash シード（`pages`/`posts`）のフィールド構成に合わせて修正（body → content、author/slug は既定で同期しないよう空欄に変更）。
